import { Typography } from '@mui/material';
import React, { useEffect } from 'react';

const Blogs = () => {

    return (
        <div>
            <Typography mt={15} variant='h2'>Comming Soon......</Typography>
        </div>
    );
};

export default Blogs;